import React, {Fragment} from 'react';

const Image = (props) => {

    const {featured_media} = props.featured_media;

    return (
        <Fragment>

        </Fragment>
    );
}

export default Image;